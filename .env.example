# 중요: VITE_ 접두사를 붙이지 마세요.
# 이 키는 Vue 프론트가 아니라 Netlify Function에서만 읽습니다.
OPENAI_API_KEY=

# 비용 민감형 텍스트 질의응답 기본 모델입니다.
# 계정에서 사용할 수 있는 다른 모델 ID로 교체할 수 있습니다.
OPENAI_MODEL=gpt-5-mini

# true로 설정하면 OpenAI를 호출하지 않고 JSON 검색 결과만으로 답합니다.
# API 키 없이 UI와 검색 기능을 먼저 확인할 때 사용하세요.
CHATBOT_FORCE_FALLBACK=false
